# Project Timeline

Flat tracker, owned by the Coordinator (`.claude/agents/gtm-coordinator.md`,
`AGENTS.md` §A0). Updated on every dispatch/handoff, not just at intake.
Target dates are never invented — anything blocked on a user decision reads
`BLOCKED (User Decision)` until the user supplies one. Seeded 2026-09-26 from
a full inventory of open worktrees/issues/PRs. **Refreshed 2026-09-26
(second pass, same day)** against a fresh `git worktree list` / `gh issue
list` / `gh pr list` / `gh run list` sweep — see backlog.md's "Coordinator
sweep" note for the full evidence trail. **Rows 18-20 and 22-25 updated
again 2026-09-26 by two concurrent Coordinator passes** (this file is being
actively edited by more than one session today — each pass below is dated
in its own Status cell rather than claimed as one global sweep): one pass
resolved issue #278's UAT hold and QA-verified PR #1/#2/#3; a second pass
(rows 18-20) resolved issue #39's brand-spec conflict directly with the
owner and ran the Product Owner UAT gate on `ps-brand-overhaul-2`, opening
PR #46 — see `docs/backlog.md`'s 2026-09-26 entries and
`docs/agent-artifacts/uat-ps-brand-overhaul-2-brand-tokens-shapes-type.md`.
**A third pass this same day** dispatched BA+Architect co-scoping order for
issues #26/#30/#33/#35 — see `docs/backlog.md`'s "Coordinator dispatch —
2026-09-26 (third pass)" entry. **A fourth pass this same day** processed
three owner decisions on that third pass's findings (issue #48 filed for
the "Default industry key" decision; issue #33 closed, split into #49/#50;
`ps-brand-overhaul` parts 3–7 re-sequenced ahead of #26/#30) — see
`docs/backlog.md`'s "Coordinator dispatch — 2026-09-26 (fourth pass:
owner decisions on #33/#35/ps-brand-overhaul)" entry for full evidence.

| Item | Stage | Target Date | Owner (agent role) | Status |
|---|---|---|---|---|
| Issue #31 — Retire Engagement Links tab | PR #44 open (`agent/issue-31` → main) | TBD | QA (CI running, then Product Owner UAT) | **Updated** — pushed since last pass; PR #44 opened 2026-09-26T17:45. CI run `36260148426` was `in_progress` at check time; `mergeStateStatus: UNSTABLE`, `mergeable: MERGEABLE`. Do not merge until CI resolves. Not yet at the UAT gate. |
| Issue #21 + #27 — combined (renamePage guard + publish-guard) | PR #43 open (`agent/issue-27` → main) | TBD | QA (then Product Owner UAT) | **Updated** — Developer combined both fixes on one branch per a reconciliation commit; PR #43 is CLEAN/MERGEABLE, both checks SUCCESS. Ready for QA verification pass, not yet UAT'd. |
| Issue #19 — GUS availableIndustries leaks Draft industries | PR #42 open (`agent/issue-19` → main) | TBD | QA (then Product Owner UAT) | Unchanged — CLEAN/MERGEABLE, both checks SUCCESS. Still awaiting the UAT gate before `agent-workspace.sh complete` runs. |
| Issue #38 — Setup checklist detectors | PR #45 open (`agent/issue-38` → main) | TBD | Developer (rebase dispatched by owner directly) | **Changed (verified independently, third pass)** — worktree `issue-38` has since been removed (normal post-`agent-workspace.sh complete` cleanup) and **PR #45 is now open**. `gh pr view 45` showed `mergeStateStatus: DIRTY`, `mergeable: CONFLICTING` (main has moved since the branch point). **Owner is dispatching a Developer to rebase this directly, outside the Coordinator's roster work this pass** — noted here for timeline accuracy only, not actioned by the Coordinator. |
| Issue #39 — Migration Accelerator overhaul (PS branding) | BA/Architect scoping in progress across parts 1–7; PR #40 (initial audit) still open | TBD | BA/Architect/Coordinator | **Unblocked 2026-09-26.** All three brand-spec forks on this issue are now resolved: (1) Configurator chapter count → keep 9 distinct, (2) proof-content disclosure → keep anonymized/no Medtronic naming (both decided earlier the same day, recorded in the issue's own GitHub comments — not re-litigated here), and (3) the color/font/shape conflict between `ba-scope/issue-ps-brand-configurator` and the `ps-brand-overhaul` initiative → **the `ps-brand-overhaul` spec (Radiant Red `#E90130`; Lexend Deca/Roboto/Roboto Mono; fully-rounded pills; red never fills a button) is authoritative**, resolved directly by the owner (AskUserQuestion), sourced from the full 44-page "PS Brand Guidelines 04.2026" PDF. `ba-scope/issue-ps-brand-configurator` is now formally superseded — see `docs/backlog.md` 2026-09-26 entry and https://github.com/iwintrose/gtm-offerings/issues/39#issuecomment-5848688802. No BA/Architect estimate exists yet for the full 7-part initiative, so Target Date stays TBD rather than BLOCKED. **Owner confirmed this pass:** `ps-brand-overhaul` parts 3–7 now take priority over issues #26/#30 in the next dispatch cycle. **Open escalation, not yet routed to a part:** a real, live, pre-existing violation of the just-confirmed "red never fills a button" rule (`gtmConfigurator.css`'s `.cta`/`.tbtn-gus` solid-red buttons) was found during part 2's UAT below; not covered by part 2's scope (correctly) or by part 6's existing scope doc (checked directly, not assumed) — needs an explicit owner call on which part owns the fix. |
| Issue #48 — Configurator "no industry selected" fallback becomes a first-class `Industry_Key__c='Default'` row | **Filed this pass by the owner** (was: unfiled, only a scope-doc reference) | TBD | BA/Architect | **New row, fourth pass.** This is the "Default industry key" decision previously tracked only in `task-scope-ps-brand-overhaul-1-default-industry-key.md` with no issue number — the owner filed #48 to give it a real tracked record, preserving that scope doc's content as "the fuller technical scoping... authoritative detail" per #48's own body. Confirmed independently via `gh issue view 48`: title, body, and sequencing note ("part 1 of 7... prerequisite for part 3") all match. This is part 1/7 of `ps-brand-overhaul` — see that initiative's row below for build status (still zero feature commits; scope doc consolidated into part 2's branch). |
| `ps-brand-overhaul-2-brand-tokens-shapes-type` (part 2/7) | **UAT ACCEPT — PR #46 open** (`agent/issue-ps-brand-overhaul-2-brand-tokens-shapes-type` → main) | TBD (CI just started) | Coordinator (UAT done) → owner (merge decision) | **Done.** QA's technical pass (1324/1327, both non-negotiable Architect corrections verified) independently re-verified by the Coordinator (fresh `npm test` run reproduced identical numbers; SVG clipPath arc-based notch and absence of ambient dark-mode confirmed by direct code read; real wiring into all 3 named consumers confirmed; zero leftover wrong-brand values confirmed via direct grep). UAT verdict: **ACCEPT** — see `docs/agent-artifacts/uat-ps-brand-overhaul-2-brand-tokens-shapes-type.md`. Pushed and PR #46 opened: https://github.com/iwintrose/gtm-offerings/pull/46. CI (`Agent CI Gate`) was `IN_PROGRESS` at last check, `mergeable: MERGEABLE`. Coordinator does not merge — owner's call. One adversarial finding carried into the PR body rather than blocking it: see issue #39's row above. |
| `ps-brand-overhaul-3..7` (parts 3–7/7, part 1 = issue #48 above) | **Re-sequenced ahead of #26/#30 this pass, per owner decision** — BA-scoped (`ba-scope/issue-ps-brand-overhaul-3` through `-7`, pushed, full task-scope docs), no Architect addendum, no worktrees | TBD | BA/Architect | **Owner call, fourth pass:** now that #39's brand-spec conflict is resolved and PR #46 (part 2) is UAT-accepted, parts 3–7 jump ahead of issues #26/#30 in the next Architect dispatch cycle; #35 (Story base content, separate from this initiative but a load-bearing prerequisite for it) stays first overall. Confirmed part 7 (industry-content-authoring) is **not** a duplicate of issue #35 — different template (`industry-profile` fields vs. `gtmStory`'s 11 base sections). Confirmed part 5 (story-restyle) is shape/token-only and excludes content copy. **Gap still unowned by any of the 7 parts:** final PS-brand copy for `gtmStory`'s 11 base sections — #35 only ports the *existing* hardcoded copy verbatim, part 5 does no content work, part 7 authors `industry-profile` fields not `gtmStory`'s base copy. Needs a part 8 or an explicit fold-in decision. |
| `issue-demo-seed-integrity-rebuild` | Scoped (worktree exists, 1 commit + 1 untracked file, unpushed) | TBD | BA/Architect | Full scope doc exists; explicitly scopes OUT authoring Migration Accelerator Configurator content (deferred to the separate, in-review ps-brand-overhaul line) to avoid a duplicate build. Untracked `scripts/data/rebuild-gtm-demo-seed.apex` sitting in the worktree — not committed. |
| `gus-chat-branding-refresh` (owner ask in chat, not a filed GitHub issue) | **MERGED + live on gtm-staging.** PR #51 merged 2026-09-26T18:59:22Z. `gtm-prod` explicitly held by the owner — staging-only for now. | Complete (staging) | Owner (holds prod decision) | **Complete for gtm-staging, 2026-09-26.** QA independently verified diff scope, Jest (1319/1322 passed, no regressions), and `sf project deploy validate` (0 errors on all 7 touched components). Coordinator ran the live-browser check QA's session couldn't (real deploy `0AfgK00000UbfHSSAZ` to `gtm-staging`): 3 of 4 named GUS surfaces confirmed correct (`gtmGusUtility`, `gtmAgentChat` — computed bubble color `rgb(233,1,48)`=`#E90130`, font `Roboto`, Google Fonts CDN link present; `gtmReadoutAssist` — mascot icon confirmed). 4th surface (`gtmAgentBubble`/Experience Cloud) blocked by a pre-existing `GTM1` site custom-domain misconfiguration unrelated to this diff (logged as a follow-up in `docs/backlog.md`). UAT verdict and full evidence: `docs/agent-artifacts/uat-gus-chat-branding-refresh.md`. PR: https://github.com/iwintrose/gtm-offerings/pull/51 (merged). A subsequent full-`main` deploy to `gtm-staging` hit an unrelated pre-existing `AuthProvider` blocker (`GTM_Employee_SSO`, present since initial import) — logged separately in `docs/backlog.md` for Coordinator to route; does not affect this task's own components, which are already live via a direct targeted deploy. **`gtm-prod`: explicitly held by the owner (2026-09-26) — do not deploy without a fresh, separate go-ahead.** |
| Issue #278 (PR #13) — restore missing gtmStory CSS lost at issue #33 | UAT ACCEPT, PR #13 pushed (rebased, force-with-lease, user-approved) | TBD | Coordinator (UAT done), owner (merge decision) | Resolved this pass. Deployed gtmStory/gtmPagePreview to gtm-staging only (not a merge), visually verified via claude-in-chrome: real card grid, boxed padding, colored badges render correctly, confirming the stacking regression is fixed. Verdict upgraded CONDITIONAL-ACCEPT to ACCEPT, see docs/agent-artifacts/uat-278.md. Branch was rebased then force-with-lease pushed (user-approved) since it was already an open PR. CI was still starting at last check. Coordinator does not merge, owner's call. |
| PR #1 — alias-length test fix (`issue-readouts-awaiting-alias-fix`) | Fixed by Developer, ready for QA re-verify | TBD | QA | Root cause was a missing `task-scope-<id>.md` (CI's scope-gate failed before tests ran); Developer authored it, confirmed the actual test fix was already correct, local checks clean. **Process gap flagged:** this PR shipped without a scope doc in the first place — worth an audit of other in-flight branches for the same hole. **Re-checked independently, third pass:** `gh pr view 1` still showed both status checks `FAILURE` — CI had not yet confirmed the fix this row claims; don't treat as resolved until a fresh green run is observed. |
| PR #2 — server completeness gate (`hasMeaningfulAnswers`) | MERGED | Done | Merged | UAT ACCEPT (docs/agent-artifacts/uat-2.md), owner-approved, merged (squash) into main 2026-09-26T19:02:32Z. Branch deleted. |
| PR #3 — ANSWER_FIELDS dedup | MERGED | Done | Merged | UAT ACCEPT (docs/agent-artifacts/uat-3.md), owner-approved, merged (squash) into main 2026-09-26T19:04:01Z. Branch deleted. |
| Merged-but-not-cleaned-up worktrees | Done, needs pruning | N/A | Coordinator (housekeeping) | `issue-industry-variant-ui-scoping-fix` (PR #32, merged, still has a stray uncommitted `TEST_FAILURES.log`), `issue-gus-live-agentforce-provider-auth` (PR #34, merged), `issue-industry-variants-agentforce-draft` (PR #28, merged), `issue-industry-variants-visibility-nav` (PR #23, merged). None represent live work; all are stale worktrees safe to `git worktree remove` once confirmed nothing uncommitted is lost — not yet done. |
| Issue #4 — Contentful authoring-surface migration | Backlog (B16, open questions) | BLOCKED (User Decision) | Coordinator | Unchanged. |
| Issue #33 — Rewire GUS onto real Agentforce runLoop | **CLOSED by owner this pass, referencing PR #36** | N/A | Coordinator (recommendation actioned) | **Owner decision, fourth pass, verified via `gh issue view 33 --json state,closedAt,stateReason` → `CLOSED`/`COMPLETED`/`2026-09-26T18:39:27Z`.** Matches this Coordinator's third-pass recommendation exactly: PR #36 already delivered the configurator-surface Agent API swap (Fork 1 + Fork 2 of the Architect addendum). Split into two narrower follow-ups, both confirmed filed and open: **#49** (Agentforce Topics/Actions tool-parity for `GTM_Configurator_Assistant` — it has zero wired actions today) and **#50** (extending the live Agentforce provider beyond the configurator surface to `chatOnReadout`/`chatOnApp`, explicitly noted in #50's own body as likely dependent on #49). Neither #49 nor #50 has a BA co-scoping pass yet — both `TBD`/Backlog, added as new rows below. |
| Issue #49 — Agentforce Topics/Actions tool-parity for GTM_Configurator_Assistant | Backlog | TBD | BA | New row, fourth pass. Split from closed #33. Scope: build Agentforce Actions/Topics in Agent Builder/Agent Script equivalent to GUS's existing registered Claude tools (`GtmFilterByQueryAppTool` and siblings), calling the same underlying Apex methods, so tool-call results are consistent regardless of active provider. Explicitly out of scope: extending Agentforce to other chat surfaces (tracked in #50). Not yet scoped by a BA. |
| Issue #50 — Extend live Agentforce provider beyond the configurator surface (chatOnReadout, chatOnApp) | Backlog | TBD | BA | New row, fourth pass. Split from closed #33. Scope: decide whether PR #36's Agent API session/messages transport pattern extends directly to `chatOnReadout()`/`chatOnApp()`, or needs its own session-lifecycle handling; confirm per-surface tool/data-access requirements. **Own body flags a real dependency: likely blocked on #49 landing first** (broader/different tool surface needed for app-wide/readout chat). Not yet scoped by a BA — sequence after #49 when both reach the front of the queue. |
| Issue #35 — Author base content for Story page | **Coordinator dispatching BA+Architect co-scoping — 1st priority, unchanged this pass** | TBD | BA | Issue #35's actual filed text explicitly wants these 11 `gtmStory` base rows created **with NO `Industry_Key__c`** — the opposite of "Default as a first-class value" (that decision is issue #48, filed separately this pass — see its row above; conflating the two earlier was a citation error, now corrected and resolved via #48's filing). #35 itself is unblocked, well-specified, and load-bearing: `task-scope-39.md`'s sequencing note says explicitly "Story base content (closing #35) must land before Story industry variants are touched." **Owner confirmed this pass: #35 stays first overall**, ahead of the now-reprioritized `ps-brand-overhaul` parts 3–7 and ahead of #26/#30. |
| `ps-brand-overhaul-3..7` — see row above (re-sequenced ahead of #26/#30) | | | | |
| Issue #26 — Rebuild gtmHeatGrid on lightning-datatable | **Coordinator dispatching BA+Architect co-scoping — slides to 3rd priority this pass, behind `ps-brand-overhaul` 3–7** | TBD | BA | Well-specified in its own filed text (concrete component inventory, cites the Salesforce-first standing rule, an already-live `gtmLinkDatatable` precedent to reuse). No dependency on the branding initiative. **Owner call, fourth pass:** slides behind `ps-brand-overhaul` parts 3–7 now that #39 is unblocked, per explicit owner re-sequencing instruction. |
| Issue #30 — GUS utility bar overlaps native Help | **Coordinator dispatching BA+Architect co-scoping — slides to 4th priority this pass, behind `ps-brand-overhaul` 3–7 and #26** | TBD | BA | Needs more upfront research than #26/#35 (native Help-bubble mechanism unconfirmed, both FAQ pages empty in `gtm-staging`, "Agentforce-powered" is ambiguous). **Cross-reference for the BA:** issue #33 (closed) established Agentforce is configurator-surface-only today; #50 (extending it to `chatOnApp`, the utility bar's own entry point) may need to land first, or #30's BA scope needs to explicitly treat Agentforce-powered Help as dependent on #49/#50 rather than assuming it's available. **Owner call, fourth pass:** slides to last of this pass's four, behind `ps-brand-overhaul` 3–7 and #26. |
| Issue #15 — Converge tile system | Backlog | TBD | BA | Not yet scoped. |
| Issue #14 — Extract c-gtm-card | Backlog | TBD | BA | Not yet scoped. |
| Issue #11 — Backfill Account lookups for typed-Company rows | Backlog ("Later", unscheduled) | TBD | BA | Not yet scoped. |
| Issue #10 — Ask GUS in filter bar | Backlog ("Later", unscheduled) | TBD | BA | Not yet scoped. |
| Issue #7 — Implement D11 resolution plan | QA (per backlog.md, implemented/unit-tested on main) | TBD | QA | QA verification protocol not yet run live — see backlog.md D11. |
| Issue #6 — Implement Content Manager IA plan | Backlog | TBD | BA | Not yet scoped. |
| Issue #5 — Verify Salesforce self-approval of Readout | Backlog | TBD | BA | Not yet scoped. |
| R1 — 72 migrated industry rows never reviewed | Needs Don's eyes (backlog.md) | BLOCKED (User Decision) | Coordinator | Waiting on user review. |
| R2 — BOA/SC-0012 lost payload | Needs Don's eyes (backlog.md) | BLOCKED (User Decision) | Coordinator | Needs manual re-entry by user, not recoverable by an agent. |
| D14 — Instrument Editor previews wrong band ladder for no-instrument offerings | Backlog (deliberately unfixed) | BLOCKED (User Decision) | Coordinator | Needs a decision on what a no-frame offering should preview. |
| Guided Setup — remaining QA browser pass on gtm-staging | QA | TBD | QA | Section 10 of guided-setup.md, not yet run. |

## Notes

- This table reflects four interleaved same-day Coordinator passes on
  2026-09-26 (see the dated callouts above the table and in each Status
  cell). It is not a live feed — the Coordinator refreshes it on each
  dispatch/handoff it observes, and more than one Coordinator session has
  been active on this file today; each pass scopes its edits to the rows it
  owns rather than re-writing the whole table.
- "TBD" (not yet scoped, or scoped but no confirmed date yet) is not the
  same as "BLOCKED (User Decision)" — TBD means a BA/Architect co-scoping
  pass hasn't happened or hasn't produced a date; BLOCKED means the work
  cannot even be scoped/merged until the user answers something. Issue #39
  moved from BLOCKED to TBD for exactly this reason: it is now
  scopable/buildable, just without a confirmed date for the full
  initiative yet.
- **Current dispatch order, as of the fourth pass, owner-confirmed:**
  Issue #35 (1st) → `ps-brand-overhaul` parts 3–7 / issue #48 (2nd) →
  Issue #26 (3rd) → Issue #30 (4th). Issue #33 is closed; #49/#50 are
  filed but not yet in the active dispatch queue (BA co-scoping order
  among #49/#50/#26/#30 to be set at the next dispatch cycle).
- No CI runs were found executing directly against `main` in the last 30
  Actions runs (`gh run list --limit 30`) — all recent runs are scoped to
  PR branches or their push events. No red build on `main` itself as of
  the second pass.
- See `docs/backlog.md` for the full narrative history behind each item,
  including the "Issue #39 brand-spec conflict resolved," "Coordinator
  dispatch — 2026-09-26 (third pass)," and "Coordinator dispatch —
  2026-09-26 (fourth pass)" entries.
- See `docs/agent-artifacts/uat-ps-brand-overhaul-2-brand-tokens-shapes-type.md`
  for the full Product Owner UAT evidence trail behind PR #46.
