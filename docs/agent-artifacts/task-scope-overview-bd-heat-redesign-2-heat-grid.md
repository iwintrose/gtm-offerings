# TASK SCOPE — ISSUE #overview-bd-heat-redesign-2-heat-grid

## 1. Requirements Breakdown

- **Target Objective:** Piece 2 of the 4-way split of `overview-bd-heat-redesign` (parent scope: branch `ba-scope/issue-overview-bd-heat-redesign`, see §4 of that doc for the full split rationale). Build the "who's hot right now" heat grid: one row per Account, columns = the last 14 days, cell shading reflecting recency + strength of engagement tier, sorted hottest-first, with the two feasible-now "Next best action" buttons wired to real actions. Sibling `-1-layout-toggle` (Today/Offerings toggle scaffold + funnel removal) is a **dependency, not yet merged** — see Dependency note below. Sibling `-3-daily-widgets` (Tasks due today / Readouts awaiting response) is independent and out of scope here. The follow-up ticket `gus-nudge-tool-and-readout-viewed-tracking` (GUS Nudge tool, readout-viewed/replied tracking) is also out of scope.

  **In scope**
  1. **New Apex aggregation** over `GTM_Link_Event__c`, grouped by Account (via `Saved_Configuration__c` → its Account/Company lookup) and by day (`CreatedDate` truncated to day) across the trailing 14 days. Bucket `Event_Type__c` into the same engaged/started/submitted tiers `GtmLinkStageService` already defines (Page View → engaged, Form Opened → started, Form Submitted → submitted) for vocabulary consistency with the (now-removed-from-the-UI-but-not-deleted) funnel logic. Read-only, no DML, capped/paginated consistent with `GtmLinkStageService`'s existing 50,000-row cap pattern. Likely lands as a new method on `GtmLinkStageService` (reuse its tier vocabulary/helpers directly) or a new sibling service class — Developer's call, but must not duplicate tier-bucket logic by copy-paste; either extend `GtmLinkStageService` or delegate to its existing bucket helpers.
  2. **Heat grid LWC** (new child component under `force-app/main/default/lwc/`, name TBD by Developer, e.g. `gtmHeatGrid`) rendering: Account, Contact, Opportunity **Stage** (real `Opportunity.StageName` picklist value, not a custom vocabulary string), a "Latest signal" text summary, the 14-day shaded cell strip, and a state-appropriate "Next best action" button — sorted hottest-first.
  3. **Reuse `c/gtmLinkDatatable`/`c/gtmLinkCell`** (shared Account/Contact cell-type component, merged to `main` via PR #260, `agent/issue-pages-table-account-column-consistency`) for the grid's Account/Contact columns, rather than reintroducing the old `type:'button'`-with-data-driven-`disabled` cell pattern that PR replaced. Do not invent a parallel cell-type mechanism.
  4. **"Call now" button** (cold/new rows): Log a Call pre-fill via standard `lightning:recordEditForm`/quick-action pattern against Task (Call type) or the native Log a Call action, pre-filled with WhoId/WhatId from the row's Contact/Opportunity. No new Apex needed — navigation/quick-action wiring only.
  5. **"Follow up" button** (started/dropped-off rows): creates a Task linked to the Opportunity (`WhatId` = Opportunity). Before writing a new Task-creation helper, grep the codebase for an existing one (e.g. inside `GtmActTodayController` or elsewhere) to avoid duplicating logic.
  6. **"Nudge" and "Book the call"'s schedule-action portion: explicitly disabled, not silently absent.** Render both as visible-but-disabled buttons with a tooltip/label pointing at the follow-up ticket (`gus-nudge-tool-and-readout-viewed-tracking` for Nudge; note in the same spot for the schedule-action piece of "Book the call" if the Developer determines the readout-deep-link portion alone is trivially feasible — check `gtmReadoutView`/readout workspace components first per the parent scope's feasibility table before assuming the whole button is out of reach). A rep must never wonder why a button does nothing with no on-page indication.
  7. **Contract doc first** (`CLAUDE.md` §4, "Contract First"): before Developer LWC/Apex work starts, `docs/architecture/` must gain a contract doc (new or as a section of the `gtmOverview` rebuild doc from `-1-layout-toggle` if that already exists) covering the heat grid's data contract — per-day bucket shape, tier definitions, sort order, 14-day window edge behavior.
  8. **Tests:** new Apex test class (`Gtm*ControllerTest.cls`/`GtmLinkStageServiceTest.cls` convention) with meaningful coverage of the tier/date-bucket boundary logic (14-day window edges), and Jest specs for the new heat-grid LWC.

  **Dependency note — read before starting build:** This slice depends on `-1-layout-toggle` (branch `agent/issue-overview-bd-heat-redesign-1-layout-toggle`) for the Today/Offerings toggle shell to exist. As of provisioning this worktree, `-1-layout-toggle` is **not yet merged to `main`** (only its scope-file commit exists on that branch; no development commits yet). Before starting: check whether `-1-layout-toggle` has merged. If it has, rebase/build the heat grid directly into the Today view of the new toggle shell. If it has not, coordinate with whoever owns that slice, or build the heat grid against the current pre-toggle `gtmOverview` structure as a self-contained component that can be slotted into the Today view in a later integration step once the toggle shell lands — do not block on it, but do not silently diverge from its shape either.

  **Explicitly out of scope for this slice**
  - The Today/Offerings toggle itself, funnel-card removal, and re-homing of offering cards — all `-1-layout-toggle`.
  - "Your tasks due today" and "Readouts awaiting response" widgets and their new Apex — `-3-daily-widgets`.
  - The GUS "Nudge" tool implementation, pre-seeded GUS context wiring, and any readout-viewed/replied tracking field — follow-up ticket `gus-nudge-tool-and-readout-viewed-tracking`, not this ticket. This slice only renders Nudge as disabled.
  - Any live `gtm-prod` data change or seed script.
  - `GtmLinkStageService.cls`'s existing consumers (e.g. Pages tab stage filter) — not modified, only extended (if the Developer chooses to add the new aggregation method there) or read from.

- **System Component Impacted:** New Apex (read-only aggregation method, likely on `GtmLinkStageService` or a new sibling service class — no DML, no callouts, confirmed no CMDT), new heat-grid LWC + Jest specs, reuse of the existing `c/gtmLinkDatatable`/`c/gtmLinkCell` shared component (already on `main` via PR #260). No new SObjects or fields — `GTM_Link_Event__c` already carries everything needed for the 14-day per-Account/per-day buckets. No Custom Metadata, no YAML instrument content, no GUS tool surface changes (Nudge is stubbed disabled, not implemented).

## 2. Code Dependency Checklist

- [ ] Modifying GUS Tool Surface? **NO.** The Nudge button is rendered disabled/stubbed in this slice; no new `GtmAppTool` is implemented here. (If a future Developer decides to implement it anyway, that is out of scope and should be rejected back to the follow-up ticket — do not let it sneak in via this ticket.)
- [ ] Altering Custom Metadata? **NO.** No `GTM_Assessment_*` metadata, no `migration-accelerator/`/`instrument/<offering-key>/` YAML touched.
- [ ] Introducing database fields? **NO.** `GTM_Link_Event__c` already has everything needed (`Saved_Configuration__c`, `Event_Type__c`, `CreatedDate`, `Contact__c`) — no new field, no new object. (The "readout viewed" gap remains explicitly unaddressed here, per the parent scope's recommendation — it is follow-up-ticket scope, not this one.)

## 3. Plan Acceptance Criteria

- **Success Metric:**
  1. New Apex read-only method(s) return per-Account/per-day aggregation over `GTM_Link_Event__c` for the trailing 14 days, bucketed into the same engaged/started/submitted tiers `GtmLinkStageService` already defines. No DML, no callouts. Apex test class covers the 14-day window boundary (day 14 included, day 15 excluded) and each tier bucket.
  2. Heat grid renders one row per Account (verified against real data — no row keyed by link or Opportunity id), 14 day-columns with recency/strength shading, sorted hottest-first, each row showing Account, Contact, Opportunity Stage (real `StageName`), and a "Latest signal" text summary.
  3. Account/Contact columns use `c/gtmLinkDatatable`/`c/gtmLinkCell` — no reintroduction of the old `type:'button'`-with-data-driven-`disabled` cell pattern PR #260 replaced. Grep confirms no new competing cell-type mechanism is introduced.
  4. "Call now" opens Log a Call pre-filled with the row's Contact/Opportunity; "Follow up" creates a Task against the Opportunity. Both are real, wired actions, not stubs.
  5. "Nudge" and the schedule-action portion of "Book the call" render as visibly disabled with an on-page explanation (tooltip/label) pointing at follow-up-ticket status — never silently missing or invisible.
  6. `docs/architecture/` contains a contract doc for the heat grid's data shape (per-day bucket shape, tier definitions, sort order) committed before/alongside the Developer's first implementation commit.
  7. No "Company" header anywhere in the new markup — Account/Contact/Opportunity Stage labeling only, consistent with PR #260's Account/Contact display-precedence fix.
  8. If `-1-layout-toggle` has merged by the time this ships, the heat grid renders inside its Today view. If not, the component is self-contained and does not hard-couple to toggle-shell markup that doesn't exist yet on `main`.
- **Target Test Target:** New heat-grid LWC's `__tests__/*` (Jest), new Apex test class following the `Gtm*ControllerTest.cls`/`GtmLinkStageServiceTest.cls` naming convention, covering tier/date-bucket boundary logic.
