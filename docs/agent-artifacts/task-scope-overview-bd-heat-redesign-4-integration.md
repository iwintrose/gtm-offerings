# TASK SCOPE — ISSUE #overview-bd-heat-redesign-4-integration

## 1. Requirements Breakdown

- **Target Objective:** Wire the three already-merged, orphaned LWCs — `c-gtm-heat-grid`, `c-gtm-tasks-due-today-widget`, `c-gtm-readouts-awaiting-widget` — into `gtmOverview`'s Today view. This is the final "integration" slice of the four-part Overview BD-heat redesign (parent: `docs/agent-artifacts/task-scope-overview-bd-heat-redesign.md`, branch `ba-scope/issue-overview-bd-heat-redesign`). Slices 1-3 built the toggle shell and the three components in isolation; none of the three is mounted anywhere in `gtmOverview.html` today, which means a rep sees none of them despite them being live, tested code sitting on `main`. The user has explicitly said they do not want to review these three components again as isolated pieces — this ticket is pure composition/wiring, not component design.
- **System Component Impacted:** LWC only — `force-app/main/default/lwc/gtmOverview/gtmOverview.html` (mount points + reordering) and `gtmOverview.css` (new two-column row layout for the daily-widgets pair). No changes to the three child components' `.js`/`.html`/internal logic are in scope. No Apex, no CMDT, no YAML instrument content.

### Verified against current code (not just the architect's summary)

- `force-app/main/default/lwc/gtmOverview/gtmOverview.html` (read directly, 324 lines): confirms the architect's description. Inside `<template if:true={isTodayView}>` (starts line 86), the current order is exactly: Row 1 "Act today" tiles (`ul.at-row`, lines 97-117) → Row 2 "Opportunities to follow up on" table, now solo-width (`div.ov-cols.ov-row2.ov-row2--solo`, lines 122-190) → Row 3 "Assessment results" table (`div.ov-cols.ov-row3.ov-row3--solo`, lines 194-229). None of the three new components appear anywhere in this file. The Offerings view (line 236+) is untouched by this ticket.
- All three components' `.js` files carry near-identical header comments confirming the orphaned state directly in their own source (not just in the architect's notes): `gtmHeatGrid.js` — "NOT wired into gtmOverview.html yet... can be mounted as `<c-gtm-heat-grid></c-gtm-heat-grid>` anywhere a Today view slots it in later"; `gtmTasksDueTodayWidget.js` and `gtmReadoutsAwaitingWidget.js` both say "NOT yet wired into gtmOverview.html because the Today/Offerings toggle shell it belongs inside... had not landed on main at the time this was built." That shell (`overview-bd-heat-redesign-1-layout-toggle`) has since landed on `main` — this ticket is the deferred wiring step all three were written anticipating.
- All three self-load in `connectedCallback` via their own Apex imperative call (`GtmLinkStageService.getHeatGridAura`, `GtmTasksDueTodayController.getTasksDueToday`, `GtmReadoutsAwaitingController.getReadoutsAwaiting`), expose no `@api` properties, and fire no custom events. Confirmed by reading each `.js` file directly. This means mounting is genuinely bare-tag composition — `<c-gtm-heat-grid></c-gtm-heat-grid>`, `<c-gtm-tasks-due-today-widget></c-gtm-tasks-due-today-widget>`, `<c-gtm-readouts-awaiting-widget></c-gtm-readouts-awaiting-widget>` — with no prop wiring or parent-side event handlers to write.
- `gtmReadoutsAwaitingWidget.js`'s header comment independently confirms the "no viewed/replied" copy constraint the parent scope doc's gap analysis already flagged: "Copy here MUST stay 'sent N days ago, no update since' and must never say 'viewed' or 'replied' as fact." This is baked into the component already (not something this ticket introduces or needs to re-litigate) — integration must not touch that widget's internal copy.
- Confirmed via `gtmOverview.js` (grep): `isTodayView`/`isOfferingsView` getters (lines 627-628) and `showActTodayRow` (line 555, currently hardcoded `return true`) are the existing conditionals this ticket's new markup sits alongside — no new JS state is needed to gate the three new components; they render unconditionally whenever `isTodayView` is true, same as the existing Act Today block.

### Target layout — ordering confirmed, locked in

Per the user-approved mockup (an Artifact, referenced by the invoking Architect, not re-derivable from code): inside `<template if:true={isTodayView}>`, the order becomes:

1. `<c-gtm-heat-grid>` — hero position, **first** inside the Today view, above the existing Act Today tiles row. This is a genuine reorder of existing content, not a pure insertion: today Act Today is first; after this ticket, the heat grid is first and Act Today drops to second.
2. Existing "Act today" tiles row (`ul.at-row`) — unchanged markup, now second instead of first.
3. New two-column row: `<c-gtm-tasks-due-today-widget>` + `<c-gtm-readouts-awaiting-widget>` side by side.
4. Existing "Opportunities to follow up on" table (`ov-row2--solo`) — unchanged, content and position relative to Assessment results unchanged.
5. Existing "Assessment results" table (`ov-row3--solo`) — unchanged, last.

This ordering (heat grid above Act Today, not below it) is the one real layout decision in this ticket and is being locked in as specified by the Architect's brief, which cites the user-approved mockup directly. If whoever builds this has any doubt the mockup still says this — e.g. if the Artifact has since been revised — they must re-check the mockup before implementing, not assume this doc is self-evidently right; this doc is not the mockup, it is a transcription of it.

## 2. Code Dependency Checklist

- [ ] Modifying GUS Tool Surface? **NO.** No GUS tool surface touched — pure LWC composition.
- [ ] Altering Custom Metadata? **NO.** No CMDT, no `instrument/<offering-key>/` YAML touched.
- [ ] Introducing database fields? **NO.** All three components already exist and already read from existing Apex/objects (`GTM_Link_Event__c` via `GtmLinkStageService`, native `Task`, `GTM_Readout__c`). No new schema.

## 3. Plan Acceptance Criteria

- **Success Metric:**
  1. `gtmOverview.html`'s Today view renders all three components in the exact order above: heat grid first, Act Today tiles second, the tasks-due/readouts-awaiting pair third (side by side, not stacked full-width), follow-up table fourth, assessment results fifth.
  2. None of the three child components' internal `.js`/`.html`/CSS is modified. If wiring them in surfaces a real conflict (e.g., a CSS class collision with `gtmOverview.css`, a component that turns out to need a prop/event after all), the Developer flags it back rather than silently patching the child component's internals to fit.
  3. The existing "Act today" tiles, "Opportunities to follow up on" table, and "Assessment results" table are unchanged in content, markup, and behavior — only their vertical position in the DOM changes (Act Today moves from 1st to 2nd; the other two keep their relative order, just pushed down by the two new insertions above them).
  4. New CSS for the two-column widget row: a flex/grid container (e.g. `.ov-daily-widgets` with `display: grid; grid-template-columns: 1fr 1fr; gap: <existing ov spacing token>;`) holding `c-gtm-tasks-due-today-widget` and `c-gtm-readouts-awaiting-widget` as equal-width siblings, consistent with the existing `.ov-cols` row-width/card-chrome conventions already used by the follow-up and assessment-results rows (reuse `ov-card`/`slds-card` chrome where the two widgets need a card wrapper, rather than inventing a new visual style) so the row doesn't look like a foreign UI pattern bolted onto the rest of the page. Below typical desktop breakpoints already established elsewhere in `gtmOverview.css` (check existing `@media` rules before adding new ones), the two columns should stack to one column rather than compress unreadably — match whatever responsive convention the rest of the page already uses.
  5. `isOfferingsView` / the Offerings view content is completely untouched by this ticket.
  6. Offering-of-record checks: no new Apex, no new CMDT, no new permission-set changes needed (all three components already ship with their own Apex/controllers and were already deployed as part of slices 2 and 3).
  7. Jest coverage: `gtmOverview`'s existing Jest suite is extended (not replaced) with assertions on the new render order — specifically that `c-gtm-heat-grid` renders before the Act Today tiles in the Today view's DOM order, that the daily-widgets row renders both child components, and that the follow-up/assessment-results tables still render after them in the same relative order as before. Existing Jest specs asserting Act Today / follow-up table / assessment results content must continue to pass unmodified in substance (selector/DOM-order assumptions may need updating if they assumed Act Today was literally the first child).
- **Target Test Target:** `force-app/main/default/lwc/gtmOverview/__tests__/*` (extend existing specs with render-order assertions covering the three newly-mounted components; no new Apex test classes needed since no Apex changes are in scope).
