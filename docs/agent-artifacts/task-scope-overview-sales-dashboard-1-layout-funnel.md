# TASK SCOPE — ISSUE #overview-sales-dashboard-1-layout-funnel

## 1. Requirements Breakdown

- **Target Objective:** Piece A of the 3-way split of `overview-sales-dashboard` (parent scope: branch `ba-scope/issue-overview-sales-dashboard`; approved mockup https://claude.ai/artifact/8yVFNqJsvPPGaqHAVRDZYa, which the BA could not load, so the owner-reviewed written spec is ground truth). Siblings B (`...-2-act-today`, Row 1 tiles) and C (`...-3-offerings-results`, Row 3) are written in parallel and are OUT of scope here. This piece delivers the layout shell, the funnel, and the follow-up table, on the shared server stage service. Hard dependency `stage-filter-pages-tab` is MERGED (origin/main `6df0e3c`; the BA's local `main` is behind origin, so the Architect must branch from `origin/main`).

  **In scope**
  1. **Removals in `gtmOverview`:** the middle "New assessment requests" panel (`.ov-col--work`, `requestsPanelTitle`, `dealStageRows`/`hasDealStageRows`/`showEmptyDealStageRows`, `isRequestedStageSelected`, `hasRequests`/`showEmptyRequests` and the requests-row rendering), `selectedFunnelStage` state and `handleFunnelStageClick`, the `asides` block and its CSS, the 5-accounts-per-page pagination (`dealsPage`, `DEALS_PAGE_SIZE`, `dealsPageCount`, `dealsPageGroups`, `dealsPagination`, `showDealsPagination`, prev/next handlers, `.deals-pagination*`; the row flattener at ~line 289 iterates `dealGroups` instead of `dealsPageGroups`), and the old `get funnel()`/`funnelShape`/`funnelReady` client-side funnel plus its now-unused `.fn-labels`/`.fn-funnel`/`.fn-shape--*`/`.fn-chip--*`/`.fn-step*` CSS. Delete (not skip) the tests for them: `describe('funnel drilldown (issue overview-redesign-funnel-drilldown)')` (~lines 625-806 of `gtmOverview.test.js`) and the pagination/asides tests (~909-952). Grep `gtmOverview.repDirect.test.js` and the rest of the file for collateral references. Rename the funnel title "How far pages get" to "From link to assessment" (subtitle "Links you sent" plus the window wording below).
  2. **Row 2:** two cards side by side at the SAME fixed height (~470px). One shared CSS class or custom property (e.g. `--ov-row2-h: 470px`) drives both, so they cannot diverge. Grid stacks below the existing 1100px breakpoint (Architect decides whether stacked cards keep the fixed height; recommend yes, each keeps 470px).
     - LEFT, the funnel: solid stacked contiguous trapezoid, count INSIDE each segment, stage name plus drop-off note beside it. Segment colours are the owner-approved palette exception: approx `#4a9fe0`, `#1e3560`, `#8fd9d6`, `#4aa79b` (in stage order sent, engaged, started, submitted; the Architect fixes the exact order/hex from the mockup). Exception scope is ONLY those four plus readable label colours (white on `#1e3560`/`#4a9fe0`/`#4aa79b`, dark on `#8fd9d6`; Architect verifies AA). Any other new hex still fails QA. Drop-off notes are positive-phrased ("9 went quiet after the send"), never "did not".
     - RIGHT, "Opportunities to follow up on": the EXISTING grouped Account > Contact > Link table (`dealGroups`/`dealRows`) with the approved 3 columns (link/offering, Opportunity, Assessment), a small engagement chip under the offering name (from `DealSummary.views`/`lastActivity`), and, OPTIONALLY, the close date under the stage pill (`DealSummary.closeDate`; the owner may drop it, so it lives in ONE isolated template block that is trivially removable; QA report must flag it). Card body scrolls internally with a sticky column header. Owner rule: this card is never taller than the funnel card.
  3. **Funnel data source (hard rule):** counts come from the merged shared service `GtmLinkStageService.getStageCountsAura()` (`@AuraEnabled(cacheable=false)`, returns `StageCounts{sent, engaged, started, submitted, quiet, hot, notOpened, truncated}`; verified against `origin/main:force-app/main/default/classes/GtmLinkStageService.cls` and `docs/architecture/gtm-link-stage-filter.md`). NOT client-side over `getDeals()`. Mapping: Links sent = `sent`, Prospect engaged = `engaged`, Assessment started = `started`, Assessment submitted = `submitted`. Drop-off notes derive from the same counts (sent-engaged, engaged-started, started-submitted; invariant `submitted <= started <= engaged <= sent` is guaranteed by the service, so differences are never negative; still clamp defensively). No second query. The service's universe is: owner-scoped (unless `viewAllRecords`), `Presentation_Stage__c` not null, not `Draft`, not `Rep_Direct`, no Opportunity requirement, inactive links included, no date window. Therefore:
     - The funnel footnote states that Draft and Rep_Direct links are excluded.
     - The parent scope's "last 90 days" subtitle is NOT supported by the service (no window exists). Do not show "last 90 days" unless the service is changed; subtitle should say what is true (e.g. "Links you have sent"). Flagged as fork 1.
     - When `truncated` is true (universe hit 50000), show a small "counts capped" note (Architect's call on wording).
  4. **Interactivity:** each funnel segment is a click-through via `NavigationMixin.Navigate({ type: 'standard__navItemPage', attributes: { apiName: 'GTM_Pages' }, state: { c__stage: '<key>' } })` with key one of `sent|engaged|started|submitted`. The Pages tab filter is live on main. Segments are keyboard-operable (`role="button"`, `tabindex="0"`, Enter and Space), with `aria-label` including stage and count.
  5. **Salesforce-native-first (standing rule), evaluations to record in the Architect's contract:**
     - Native report/dashboard funnel chart: rejected. It cannot express child-event aggregates, distinct-session visits or "Form Submitted event OR linked request" (same reasoning as `gtm-link-stage-filter.md`); it cannot be embedded with click-through into the `GTM_Pages` `c__stage` filter and would reintroduce a second definition. The owner also approved the specific trapezoid visual. Custom trapezoid (CSS `clip-path`/flex segments in the LWC) is justified on both grounds.
     - Base components: use `lightning-icon`, `lightning-button`/`lightning-button-icon` where already used. No base funnel chart component exists.
     - Card chrome: the page currently uses hand-made `.ov-card` over `slds-card`. Architect must check whether `lightning-card` (title/icon slots, `slds-card__body`) plus SLDS utilities (`slds-grid`, `slds-grid_vertical-stretch`, `slds-scrollable_y`, `slds-m-*`, `slds-size_*`) can replace hand-made card CSS for the two Row-2 cards, keeping only the fixed-height, sticky-header and trapezoid CSS as custom. Note the existing SLDS-2 button compliance tests and that the stylesheet already overrides `.ov-cols > .ov-card` header/body padding (~css lines 222-228); switching to `lightning-card` must not regress those. The table stays custom (grouped hierarchy is not expressible in `lightning-datatable` or tree-grid without losing the account/contact/link rendering).
  6. **`getDeals()` / `countStalled()` / `stalledCount` disposition:**
     - `getDeals()` and `DealSummary` are KEPT (still feed the follow-up table; Opportunity-required, LIMIT 200, no explicit `OwnerId`, relies on `with sharing` + Private OWD; keep exactly that scoping, it is correct for the opportunity-centric table). Retired in A: every client-side use of `getDeals()` for funnel maths.
     - `getSnapshot()` stays wired in A because `requests`/`headerMeta` ("N requests waiting") still read it; the middle panel's `requests` RENDERING is removed but the `requests` array must remain, or `headerMeta` be temporarily driven by `getSnapshot().newAssessmentCount`, until B repoints `headerMeta` to its server-side count. Architect picks; do not leave `headerMeta` broken or reading 0.
     - `stalledCount`/`countStalled()` and the `asides` block: the asides UI is removed in A (page views 7d / stalled leave the page until B's tiles land). The Apex `countStalled()` and `getSnapshot().stalledCount` are LEFT IN PLACE in A and are RETIRED IN B, which replaces the stalled figure with the service's `quiet`. After B lands, two definitions of "stalled" must not coexist: `quiet` (started AND NOT submitted, where a link with a request but no Form Submitted event is submitted) versus legacy `countStalled` (Form Opened with no Form Submitted event). In A, nothing on the page may display `stalledCount`; B (or a final cleanup) deletes `countStalled`, `stalledCount`, and their Apex tests. Flagged so B's Architect owns it.
     - Rep data scoping OWD: no new SOQL is added by A. The service is `with sharing` and scopes by link owner; `getDeals()` behaviour unchanged.
  7. **Height/scroll hazards for QA:** the follow-up card needs `min-height:0` on the flex/grid child plus `overflow-y:auto` on the body for the internal scroll to work, and `position:sticky; top:0` header with an opaque background.

  **Open forks for the Architect (not settled):**
  1. Window: service has no 90-day window; either accept "all links" (recommended, keeps count == Pages list) or change the service jointly with the Pages tab. Subtitle copy depends on it.
  2. Fixed height when stacked below 1100px.
  3. `headerMeta` bridge during A (see item 6).
  4. Loading/error states: the service call is separate from `getDeals()`; funnel needs its own loading, error (banner reuse) and empty (zero links) state, independent of the deals table.
  5. `lightning-card` adoption depth (item 5).
  6. Close date block: ship it or omit (owner may drop).
  7. Palette order/hex final values from the mockup.
  8. Non-admin QA gate from the stage-filter doc (rep sees prospect-generated events in counts under Private OWD); if the counts look too low for a rep, STOP and report, do not add `without sharing`.

- **System Component Impacted:** LWC `gtmOverview` (`.html`, `.js`, `.css`, `__tests__/gtmOverview.test.js`, `__tests__/gtmOverview.repDirect.test.js` for collateral only, plus a new spec e.g. `__tests__/gtmOverview.funnel.test.js`). Consumes existing Apex `GtmLinkStageService.getStageCountsAura`; no new Apex expected (Architect may add none). `GtmHomeSnapshotController` untouched in A. Permissions: `GtmLinkStageService` classAccess already exists on `GTM_Offering_User`/`GTM_Offering_Admin`; the Architect confirms. No Experience Cloud route, no YAML instrument. Architect writes the LWC data contract into `docs/architecture/` first (CLAUDE.md §4).

## 2. Code Dependency Checklist

- [ ] Modifying GUS Tool Surface? NO. Only read-only Apex is consumed (`getStageCountsAura` is SOQL-only, no DML), so AGENTS.md §1 zero-DML rule is satisfied; nothing to verify beyond confirming A adds no DML.
- [ ] Altering Custom Metadata? NO. Nothing under `customMetadata/` or `migration-accelerator/`/`instrument/` is touched.
- [ ] Introducing database fields? NO. No new fields, objects or tabs, so no Permission Set mapping is required. If an unavoidable field appears, CLAUDE.md §6 applies (grant in `GTM_Offering_User` and `GTM_Offering_Admin`).

## 3. Plan Acceptance Criteria

- **Success Metric:** QA passes only when all hold. Jest: (1) Regions/order: Row 2 renders funnel card beside follow-up card; no middle "New assessment requests" panel, no `asides`, no pagination controls even with more than 5 account groups, "Offerings" left where it is (moved by C). (2) Fixed height: both Row-2 cards carry the same shared height class/custom property; follow-up body has the scroll container class; header row has the sticky class. (3) Funnel: four segments in order Links sent / Prospect engaged / Assessment started / Assessment submitted; count inside each; counts come from the mocked `getStageCountsAura` and NOT from `getDeals()` (test with a deals mock that disagrees, and with counts above 200); drop-off notes derive from count differences, positive phrasing, never contain "did not"; footnote states Draft and Rep_Direct links are excluded; title reads "From link to assessment" and the old "How far pages get" is gone; funnel error/empty states independent of the table. (4) Click nav: each segment fires `NavigationMixin.Navigate` with exactly `{type:'standard__navItemPage', attributes:{apiName:'GTM_Pages'}, state:{c__stage:'sent'|'engaged'|'started'|'submitted'}}`; Enter and Space also navigate; roles/tabindex present. (5) Table: 3 columns, engagement chip present, close-date block present in its single isolated block, existing account/contact/link/opportunity/assessment click handlers still pass. (6) Removal: `selectedFunnelStage` and `handleFunnelStageClick` gone; old drilldown and pagination tests deleted, not skipped; `headerMeta` still correct. (7) Retained: wizard first-child mount rule, header button set, FAQ panel, rep-direct picker, SLDS-2 button-compliance tests, `gtmOverview.repDirect.test.js`. (8) Palette: exactly the four approved hex values (plus label colours) are new; no other new hex; QA must not fail the build for these four. (9) Nothing on the page displays a `stalledCount`, and `getDeals()` SOQL/scoping is unchanged. Live browser validation (standing QA rule, `gtm-dev` is Production, read-only verification only): Overview as rep and as admin; clicking each funnel segment lands on the Pages tab pre-filtered with list length equal to the count clicked (central acceptance test); the follow-up card never exceeds funnel card height and scrolls internally with sticky header; stacked layout below 1100px; existing wizard, story/feedback and table drill-ins still work. QA report flags the optional close-date block.
- **Target Test Target:** Jest: `npx sfdx-lwc-jest -- force-app/main/default/lwc/gtmOverview` (specs `gtmOverview.test.js`, `gtmOverview.repDirect.test.js`, and the new `gtmOverview.funnel.test.js`). Apex: no new Apex; regression only via existing `GtmLinkStageServiceTest` and `GtmHomeSnapshotControllerTest` (unchanged; run only per the QA gate, noting it targets `gtm-dev` = Production). CI header `ISSUE #overview-sales-dashboard-1-layout-funnel` must appear in the required places.
