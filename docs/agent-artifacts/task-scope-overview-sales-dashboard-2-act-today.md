# TASK SCOPE — ISSUE #overview-sales-dashboard-2-act-today

## 1. Requirements Breakdown

- **Target Objective:** Piece B of the Overview sales-dashboard split (parent scope `task-scope-overview-sales-dashboard.md` on branch `ba-scope/issue-overview-sales-dashboard`; mockup https://claude.ai/artifact/8yVFNqJsvPPGaqHAVRDZYa, not loadable by the BA, the written spec is ground truth). Adds Row 1 "Act today" to `gtmOverview`: four click-through tiles, each = count + one-line hint + first 3 account names, keyboard-operable (`role="button"`, `tabindex="0"`, Enter/Space). Merges AFTER piece A (`overview-sales-dashboard-1-layout-funnel`), which owns the layout, funnel, and removal of the middle requests panel/`asides`; this piece must be rebased on A and must not re-do A's work.

  **Tiles and click-through**

  | Tile | Definition | Count / names source | Click |
  |---|---|---|---|
  | New requests | `GTM_Assessment_Request__c.Status__c = 'New'` | new Apex method | `GTM_Assessments` with `c__astatus=new` |
  | Came back | stage key `hot` (>= 2 Page Views, latest visit/resume within 48h) | `GtmLinkStageService.getStageCountsAura().hot` | `GTM_Pages`, `state:{c__stage:'hot'}` |
  | Went quiet | stage key `quiet` (form opened, not submitted; Drop-off events neither add nor remove) | `getStageCountsAura().quiet` | `GTM_Pages`, `state:{c__stage:'quiet'}` |
  | Readout waiting | `GTM_Readout__c.Status__c` Pending Approval, or Approved and not yet published | new Apex method | `GTM_Assessments` with `c__areadout=pending,approved-unsent` |

  Nav shape for all four: `{ type:'standard__navItemPage', attributes:{ apiName:'GTM_Pages' | 'GTM_Assessments' }, state:{...} }` via `NavigationMixin.Navigate`.

  **Verified facts (checked against `origin/main` @ 6df0e3c, the local `main` checkout is stale and predates #224-#226; the Architect must `git fetch`/rebase first)**
  1. `GtmLinkStageService` IS on main (`public with sharing`, `getStageCountsAura()`, `getLinkIdsForStageAura(key)`, `StageCounts{sent,engaged,started,submitted,quiet,hot,notOpened,truncated}`, ids returned newest-link-first; contract `docs/architecture/gtm-link-stage-filter.md`). It returns COUNTS and link IDS only, NOT account names. Names for `hot`/`quiet` therefore need one resolution step (see fork 1).
  2. The assessments-tab contract is NOT on main yet. It exists in `/Users/isiwintr/Documents/Workbench/worktrees/issue-assessments-tab-filter/docs/architecture/gtm-assessments-table.md` (issue `assessments-tab-filter`, branch `agent/issue-assessments-tab-filter`, "Implemented, pending QA"). Params: `c__astatus` (multi, slug `new`), `c__atier`, `c__areadout` (multi; keys `none|draft|pending|approved-unsent|published`), `c__arange`, `c__aoffering`, `c__apreset` (`ready-to-book`). Multi values are one comma-separated param. Its `GtmAssessmentListController.getAssessmentPage(AssessmentQuery)` returns `totalCount` plus rows (`company`, `contactName`, `readoutStatus`, ...) under the generalized view-all scoping.
  3. **Degradation path (required):** until `assessments-tab-filter` merges, the `GTM_Assessments` tab ignores `c__a*` params (it is a no-op on unknown state, not an error). So the tiles must still navigate to plain `GTM_Assessments` (unfiltered) and the click must never throw. Recommended mechanism: always send the `c__a*` state; an unmerged tab simply ignores it. No feature-flag/runtime detection code (fewer moving parts); Jest covers the payload, and the QA report records which of the two behaviours was live-verified. If the Architect prefers explicit degradation, the only acceptable form is a single constant (`ASSESSMENTS_FILTERS_ENABLED`) that omits `state`, removed in a follow-up.
  4. Existing `GtmHomeSnapshotController` (`with sharing`) uses `isAdmin()` = permission-set-NAME check (`GTM_Offering_Admin`) and Private-OWD `OwnerId` clauses. The new method must NOT copy that; use the generalized view-all pattern (see section 2/3).
  5. `stalledCount`/`countStalled()` is the OLD "opened form, not submitted" definition and DIFFERS from the service's `quiet` (it counts a link with a request but no Form Submitted event; the service does not). Piece B retires it so exactly one definition exists.
  6. `headerMeta` (gtmOverview.js ~line 639) reads `this.requests.length` (`getRecentNewAssessmentRequests()`, LIMIT 5, which would cap the header at 5 and which piece A removes from the page).

  **Apex, exactly one small read-only method** (suggested `GtmHomeSnapshotController.getActToday()` or a tiny new class `GtmActTodayController`; Architect chooses, prefer a new class if `GtmHomeSnapshotController` is being trimmed by A, and record the signature in `docs/architecture/` before coding):
  - `public with sharing`, `@AuraEnabled(cacheable=false)`, SOQL only, zero DML, zero callout.
  - Returns a wrapper: `newRequests{count, names[≤3]}`, `readoutWaiting{count, names[≤3]}`, and (if fork 1 resolves as recommended) `cameBack{count, names[≤3]}`, `wentQuiet{count, names[≤3]}` produced by calling `GtmLinkStageService.getStageCounts()`/`getLinkIdsForStage('hot'|'quiet')` server-side (Apex to Apex, one Aura round trip, no restating of definitions) and resolving the first three link ids to display names (`GTM_Saved_Configuration__c` `Account__r.Name`, falling back to `Company__c`). Reference `Saved_Configuration__c` lookups on events/requests only via dynamic SOQL where the repo already does (repointable-lookup convention); a direct query of `GTM_Saved_Configuration__c` by Id set is not a lookup traversal and is fine.
  - Scoping: private `hasViewAllRecords(sobjectApiName)` (ObjectPermissions `PermissionsViewAllRecords=true` joined to `PermissionSetAssignment` for `UserInfo.getUserId()`, the rep-data-scoping-owd-2/3 pattern). True = no owner clause (admin sees all); false = `OwnerId = :me`. Do NOT call `GtmHomeSnapshotController.isAdmin()`. Check per object (`GTM_Assessment_Request__c`, `GTM_Readout__c`).
  - New requests: `Status__c='New'`, `ORDER BY CreatedDate DESC`, count via `COUNT()` (not `list.size()`), names = `Company__c` falling back to `Requester_Name__c`.
  - Readout waiting: `GTM_Readout__c` with `Status__c='Pending Approval'` OR (`Status__c='Approved'` AND `Notification_Sent_Date__c = null`); names from `Assessment_Request__r.Company__c`. See fork 2 for the "Approved and already notified" edge.

  **Repoint `headerMeta`** off `requests.length` to the server `newRequests.count` (piece A removes the `requests` state; this piece owns the replacement). Keep "N requests waiting" wording and singular/plural.

  **RETIRE the second stalled definition (exactly one: the service's `quiet`).** Remove:
  - Apex `GtmHomeSnapshotController`: `Snapshot.stalledCount`, `countStalled(Boolean, Id)`, its call in `getSnapshot()` (line ~74), and the comment cross-references to `countStalled()` at ~284/309/415 (reword to point at `GtmLinkStageService`).
  - Apex tests `GtmHomeSnapshotControllerTest`: assertion at ~203 (`snap.stalledCount == 1`), ~319 (`stalledCount == 0`, "reading the page is not starting the form"), and the whole test `stalledCountAndRecentViewsAreScopedToTheOwningRep` (~446-497; keep its `recentViews` scoping assertions in a renamed `recentViewsAreScopedToTheOwningRep` if `recentViews` survives piece A, otherwise delete with it). The stall-scenario coverage moves to `GtmLinkStageServiceTest` (already covers `quiet`).
  - LWC `gtmOverview.js`: the `asides` entry reading `s.stalledCount` (~578-580). Piece A removes the whole `asides` block; if A has already done so this line is already gone, and the Developer just confirms `grep -rn "stalledCount\|countStalled" force-app` returns nothing. Any `gtmOverview*.test.js` assertion on the stalled aside ("Stalled" label, `aside-v--warn`) also goes; delete, do not skip.
  - Acceptance grep: zero hits for `stalledCount` and `countStalled` under `force-app/`. `newAssessmentCount`/`activeEngagementCount` are NOT in scope to remove here (A/C decide); flag if they become unreferenced.

  **Salesforce-native evaluation (standing rule, recorded):** could standard components serve the tiles? (a) Report/dashboard metric or gauge tiles (Lightning Dashboard components, `lightning-report-chart`-style embeds): count only, no per-tile "first 3 account names" list, no `standard__navItemPage` click into a `c__` pre-filtered custom tab, cannot express the stage definitions (`hot`/`quiet` are computed from event aggregation, not a report filter) or the rep-vs-admin scoping the shared service enforces; a report cannot equal the Pages/Assessments list length by construction. (b) List-view/`lightning-card` counters: same limits, and list views cannot express the readout-status/latest-readout semantics (already rejected in `gtm-assessments-table.md` 1.4). Decision: tiles are custom (plain SLDS `lightning-card`-free markup or `slds-box` tiles in the existing Overview idiom) because count, names and pre-filtered navigation to a custom page must come from one server definition. Use existing SLDS classes/tokens; NO new hex (the funnel-palette exception in the parent scope applies only to piece A's four funnel colours). The Developer records this reasoning in the contract doc.

  **Open forks for the Architect (not settled here):**
  1. **Where names for `hot`/`quiet` come from.** The service exposes ids, not names. Recommended: resolve inside the new Apex method (above), leaving the service untouched. Alternative: add a `getLinkNamesForStage(key, n)` to the service (owned by the merged sibling, so a reopen of its contract). Also decide whether the LWC calls `getStageCountsAura()` itself (piece A likely already does for the funnel: reuse A's single call rather than a second one; then only names need the new method) versus the new method returning all four tiles. Whichever is chosen, counts for `hot`/`quiet` must come from the service, never re-derived, and never from `getDeals()`.
  2. **Readout tile vs filter keys disagree on one edge.** Tile per owner = Pending Approval or Approved (not Published). The `c__areadout` vocabulary has `pending` and `approved-unsent` only; an Approved readout whose notification was already sent has no key and would be counted by the tile but never appear in the filtered list (count != list, the exact failure the parent scope forbids). Recommended: define the tile as `pending` + `approved-unsent`, i.e. Approved AND `Notification_Sent_Date__c = null` ("waiting" to go out). Needs product-owner confirmation since it slightly narrows the owner's wording.
  3. **Count == list agreement for New requests / Readout waiting.** `GtmAssessmentListController` (unmerged) scopes rows via request ownership plus a queue-orphan arm and picks the latest readout per request; a plain `GTM_Readout__c` count can differ (multiple readouts per request; readout owned by the Opportunity owner rather than the request owner; queue-owned orphan readouts). Preferred design once `assessments-tab-filter` merges: the new method delegates to `GtmAssessmentListController.getAssessmentPage` (`status=['new']`, then `readout=['pending','approved-unsent']`, `pageSize=3`, take `totalCount` and `rows[].company`) so the tile and the table are the same code path. Until it merges, implement standalone queries using the latest-readout-per-request rule and `OwnerId`/view-all scoping, and leave a code comment plus a follow-up to switch to delegation. Architect decides which; sequencing recommendation: land this piece after both A and `assessments-tab-filter`.
  4. **Readout owner** (parent fork 5): `GTM_Readout__c.OwnerId` (Opportunity owner first, then request owner, else `GTM_Readout_Triage` queue) is not always the rep who owns the request. Recommended scoping key for the tile is the REQUEST owner (matches the Assessments table); confirm.
  5. **Zero-state and truncation.** A tile with count 0 shows "0" and an "all caught up"-style hint with no names, still clickable. If `StageCounts.truncated` is true, show the count with a "+" or a note; Architect decides the copy.
  6. **First names ordering/dedup.** Suggest newest first, deduplicated by account name (two hot links on one account shown once), max 3; count stays the true link/record count. Confirm.

- **System Component Impacted:** LWC `gtmOverview` (`.html` Row 1, `.js` tile model/nav/`headerMeta`, `.css` tile styles with existing tokens only, `__tests__/gtmOverview.test.js` and possibly `gtmOverview.repDirect.test.js` for collateral; suggested new spec `gtmOverview.actToday.test.js`); Apex (new small method, in `GtmHomeSnapshotController` or a new class, plus test class; retirement edits in `GtmHomeSnapshotController` and `GtmHomeSnapshotControllerTest`); Permission sets. No Experience Cloud route, no YAML instrument, no custom metadata, consumes the merged `GTM_Pages` `c__stage` contract and the in-flight `GTM_Assessments` `c__a*` contract. Contract-first (CLAUDE.md §4): Architect writes the Apex signature and LWC data contract into `docs/architecture/` first.

## 2. Code Dependency Checklist

- [x] Modifying GUS Tool Surface? NO. The GUS stage tool is `gus-stage-filter-tool`. The new Apex is read-only (`with sharing`, SOQL only, zero DML), so AGENTS.md §1 zero-DML rule holds; QA confirms no DML statements in the new method or class.
- [ ] Altering Custom Metadata? NO. Nothing under `customMetadata/`, no `instrument/` YAML.
- [ ] Introducing database fields? NO. All fields exist (`Status__c`, `Company__c`, `Requester_Name__c`, `Notification_Sent_Date__c`, `Assessment_Request__c`, `Account__c`, `OwnerId`). If the method is a new class, CLAUDE.md §6 still applies to class access: add `<classAccesses>` for the new class to `GTM_Offering_User` and `GTM_Offering_Admin` ONLY (never `GTM_Content_*` or `GTM_Guest`); if the method is added to the existing `GtmHomeSnapshotController`, no permission-set change is needed (it is already granted). `GtmLinkStageService` is already granted to both `GTM_Offering_User` and `GTM_Offering_Admin` on main (verified), so no change is needed for it.

## 3. Plan Acceptance Criteria

- **Success Metric:** QA passes only when ALL hold.
  1. Jest: Row 1 renders four tiles in order New requests / Came back / Went quiet / Readout waiting, each with count, hint, and at most 3 names (zero-state renders "0", no names, still clickable). Counts and names come from the Apex payload (test with a `getDeals()` mock that disagrees and assert the UI shows the server values).
  2. Jest nav payloads: Came back -> `standard__navItemPage`/`GTM_Pages`/`state.c__stage==='hot'`; Went quiet -> `'quiet'`; New requests -> `GTM_Assessments` with `c__astatus:'new'`; Readout waiting -> `GTM_Assessments` with `c__areadout:'pending,approved-unsent'`; click, Enter and Space each fire exactly one Navigate; other keys do not. Degraded path: with the filter params absent/disabled the two Assessments tiles navigate to `GTM_Assessments` with no `state` (or the state the Architect chose for degradation) and do not throw.
  3. Jest: `headerMeta` uses the server New-requests count (not `requests.length`, and not capped at 5: test with count 12) with correct singular/plural.
  4. Removal: `grep -rn "stalledCount\|countStalled" force-app` returns nothing; the removed Apex assertions/test and any LWC stalled-aside tests are deleted, not skipped; only ONE stalled/quiet definition (`GtmLinkStageService` `quiet`) exists.
  5. Apex: counts and first-3 names for New requests and Readout waiting (status filter: Pending Approval yes; Approved and not notified yes; Approved and notified no; Draft and Published no; Status != New excluded); two-rep scoping (rep A sees only own, rep B only own) plus an admin/view-all user seeing both, using a permission set that grants `viewAllRecords` (NOT only one literally named `GTM_Offering_Admin`); a rep who owns the request while another user owns the readout (per fork 4); names capped at 3, count exceeds names when more than 3; no DML. If `hot`/`quiet` names are resolved here: a test that count equals `GtmLinkStageService.getStageCounts()` and the id list length.
  6. No new hex/colour; tiles use existing SLDS tokens/classes; keyboard operable with `role="button"` and `tabindex="0"`.
  7. `GTM_Offering_User` and `GTM_Offering_Admin` (only) carry `classAccesses` for any new class (`GtmLinkStageService` is already granted to both).
  8. CI header `ISSUE #overview-sales-dashboard-2-act-today` appears in the CI-required places (this scope file, contract doc, PR title/branch). Live browser validation per standing QA rule on the Overview AND dependents: click Came back/Went quiet and confirm the Pages tab arrives filtered with list length equal to the tile count; click New requests/Readout waiting and confirm the Assessments tab either arrives filtered (list length == count) if `assessments-tab-filter` is deployed, or opens unfiltered without error if not; rep and admin views. `gtm-dev` is Production: read-only verification only, no seed or destructive scripts.
- **Target Test Target:** Jest: `npx sfdx-lwc-jest -- force-app/main/default/lwc/gtmOverview` (`gtmOverview.test.js`, `gtmOverview.repDirect.test.js`, plus new `gtmOverview.actToday.test.js`). Apex: the test class for the new method (`GtmHomeSnapshotControllerTest` if added there, else a new `GtmActTodayControllerTest`) and `GtmLinkStageServiceTest` (proves the shared `quiet`/`hot` definition still holds after the retirement); run via `sf apex run test --class-names <Class> --result-format human --wait 10` only under the QA gate, noting this runs against `gtm-dev`, which is treated as Production.

## 4. ARCHITECT ADDENDUM (forks resolved; supersedes section 1 where they differ)

Contract: `docs/architecture/overview-sales-dashboard-2-act-today.md` (write-first, Developer keeps it in sync). Base: origin/main @ 7b0c624 (includes #226, #227, #228, #230). Approved checklist: no GUS surface change; no custom metadata; no new fields. New class => `classAccesses` in `GTM_Offering_User` and `GTM_Offering_Admin` only.

Fork decisions (coordinator-directed):
1. Names for hot/quiet: new `with sharing` Apex, first 3 link ids newest first from `GtmLinkStageService.getLinkIdsForStage` (Apex-to-Apex), resolved via `GTM_Saved_Configuration__c` `Account__r.Name` falling back to `Company__c`, deduped by account. COUNTS come ONLY from piece A's existing single `getStageCountsAura()` call in the LWC (do not call it twice; the new method returns names only for hot/quiet, never counts).
2. Readout waiting = `pending` + `approved-unsent`. This narrows the owner's wording (Approved-and-already-notified is excluded); the PR body must state it.
3. New requests / Readout waiting delegate to `GtmAssessmentListController.getAssessmentPage` (status `['new']`; readout `['pending','approved-unsent']`; `sortBy='submitted'`, `sortDir='desc'`; take `totalCount`). No standalone queries. Names need dedup, so request `pageSize=25` and dedupe/take 3 in Apex (count stays `totalCount`).
4. Readout scoping is the request owner, inherited from the controller.
5. Zero state: "0" + "All caught up", no names, still clickable. `StageCounts.truncated` true => count shown as "N+" (LWC, from A's existing response); `totalIsCapped` true => same "N+" for the two Assessments tiles.
6. Names newest first, dedup by account (case-insensitive on the resolved name), max 3.

Nav (verified against docs/architecture/gtm-assessments-table.md): New requests `GTM_Assessments` `{c__astatus:'new'}`; Readout waiting `GTM_Assessments` `{c__areadout:'pending,approved-unsent'}` (one comma-separated param); Came back / Went quiet `GTM_Pages` `{c__stage:'hot'|'quiet'}`. The assessments tab is merged (#227), so NO degradation flag/constant: always send state.

Retirement (exactly one quiet definition). Beyond the BA list, origin/main also has: `gtmOverview.funnel.test.js:284` (`stalledCount: 9` in the headerMeta test, delete that key and the `not.toContain('Opened, never submitted')` line only if it becomes meaningless) and the `GtmLinkStageService.cls:15` comment referencing `countStalled()` (reword). `gtmOverview.js` no longer references it (A removed `asides`). Acceptance: `grep -rn "stalledCount\|countStalled" force-app` is EMPTY. `headerMeta` currently reads `snapshot.newAssessmentCount` (A's interim); repoint it to the act-today New requests count (not capped at 5, test 12). `newAssessmentCount` may become unreferenced: flag in the PR, do not remove.

Salesforce-native first, evaluated and recorded: standard report/dashboard metric tiles and list-view counters give counts only; they cannot supply first-3 account names, the hot/quiet stage definitions (event-aggregated, not a report filter), rep-vs-view-all scoping, latest-readout-per-request, the queue-orphan arm, or `c__` pre-filtered navigation to custom tabs. Tiles stay custom SLDS markup (existing tokens, no new hex).

DEPLOY-ORDER DEPENDENCY (hard): prospect-generated `GTM_Link_Event__c` rows are guest-owned and invisible to non-admin reps under Private OWD, so hot/quiet read 0 for reps until issue `link-event-owner-fix` lands. Developer may build and validate now, but this change MUST NOT be deployed to rep-facing prod before `link-event-owner-fix` is deployed. QA notes that admin-only verification will not reveal the defect. Record in the PR body.

Developer constraints: avoid Apex reserved words as identifiers (e.g. `limit`, `offset` as locals is fine only as fields already used; do not name variables `new`, `select`, `group`, `order`, `date`, `time`, `count`, `first`, `last`, `type`, `trigger`, `transaction`, `system`, `when`, `list`). Live deploy to gtm-dev is NOT authorised for Developer/QA. Only a validate-only run is allowed: `sf project deploy start --dry-run` (or `scripts/deploy.sh <alias>` equivalent check-only) WITH `--test-level RunSpecifiedTests` and explicit `--tests` (GtmActTodayControllerTest, GtmLinkStageServiceTest, GtmHomeSnapshotControllerTest, GtmAssessmentListControllerTest), which executes and rolls back. Never `-c` (it is --ignore-conflicts). No sf apex run test against the org, no seeds.
