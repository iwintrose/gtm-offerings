# TASK SCOPE — ISSUE #overview-sales-dashboard-3-offerings-results

## 1. Requirements Breakdown

- **Target Objective:** Piece C of the Overview "sales dashboard" redesign (parent scope: `ba-scope/issue-overview-sales-dashboard`, owner-approved mockup https://claude.ai/artifact/8yVFNqJsvPPGaqHAVRDZYa, which the BA could not load; the parent's written spec is the ground truth). Delivers **Row 3** of `gtmOverview` only. Merges AFTER piece A (`...-1-layout-funnel`, owns the row/grid skeleton and removal of the old panels); may run in parallel with piece B (`...-2-act-today`). Do not touch Rows 1-2, the funnel, the deals table or the Act-today tiles.

  **C1. Offerings, moved UP.** The existing "Offerings" block (`offeringCards` getter, `gtmOverview.js` ~line 656, fed by `GtmPageContentController.getHomeSummary`) moves from the bottom of the Overview into Row 3 LEFT. Card content, the story (Read-the-story) and feedback behaviour, and their SLDS-2 button-compliance tests are UNCHANGED. New: each card shows a **live-link count** and a **submitted-assessment count** for that offering.
  - `getHomeSummary` knows nothing about links or requests, so a small NEW read-only `with sharing` Apex method returns per-offering counts, e.g. `getOfferingCounts()` returning `List<OfferingCount{ offeringKey, offeringLabel, liveLinkCount, submittedAssessmentCount }>`. Architect names the class/method and writes the signature into `docs/architecture/` first (CLAUDE.md §4).
  - **Live-link universe = the same universe rules as the merged `GtmLinkStageService`**: `Presentation_Stage__c != null AND != 'Draft' AND != 'Rep_Direct'` (Rep_Direct and Draft excluded), grouped by `GTM_Saved_Configuration__c.Offering__c`, owner-scoped (`OwnerId = :UserInfo.getUserId()`) unless the running user has view-all on the object (`hasViewAllRecords` style check). Whether "live" also requires `Active__c = true` (the parent scope's fork 9 said so) is an OPEN FORK: the stage-service universe does not filter on `Active__c`. Recommendation: follow the stage service exactly (one universe, count matches what the Pages tab can list) and add `Active__c = true` only if the Architect confirms the service does. Do not invent a third definition.
  - **Submitted-assessment count** = `GTM_Assessment_Request__c` grouped by `Offering_Key__c` under the same owner scoping as the results card, so the card count equals the number of results-list rows for that offering. Architect decides whether it is a second aggregate in the same method or derived from the shared assessment method (fork 2).
  - **Offering label**: via `GTM_Offering__mdt` with a raw-key fallback when no record matches (existing convention, same as `getHomeSummary`/`getOfferings`). Offerings with zero links/assessments still show their card with 0 counts (never dropped). Counts load separately from `getHomeSummary`, so a failed/slow counts call must degrade to cards without counts, not blank the section.

  **C2. "Assessment results" card (Row 3 RIGHT)** = a **RANKED LIST** of the rep's submitted assessments, replacing the bar chart in the mockup. Columns: company, tier, score, request status. Best fit first: tier rank Fast-Track(4) > Accelerator-Ready(3) > Prep Required(2) > Discovery First(1) > null (0, last, shown as an em dash, never dropped), then `Assessment_Score__c` descending (null score last), then newest first (coalesce(submitted, created) desc, Id desc tiebreak). Rank is an **explicit `Map<String,Integer>`** in Apex, never a string sort or picklist position (`Assessment_Tier__c` is restricted, `sorted=false`, ascending order). Tier bands verified against `Assessment_Tier__c` and `GtmAssessmentScoring`: 27-32 / 21-26 / 15-20 / 8-14. Note tier is not derivable from score (a `source_access` answer of 1 caps at Discovery First), so sort tier first.
  - ~25 rows, fixed-height card with internal scroll (same height treatment as Row 2 cards where piece A defines a shared class/custom property; consume it, do not add a second one).
  - Each row opens that assessment using the existing idiom (`handleAssessmentClick`, ~`gtmOverview.js:368-381`): `NavigationMixin` `standard__navItemPage` `GTM_Assessments`, `state: { c__assessmentRequestId: <id> }`.
  - A **"View all"** link opens the Assessments tab pre-filtered to the `ready-to-book` quick view using the contract from issue `assessments-tab-filter`: `{ type:'standard__navItemPage', attributes:{apiName:'GTM_Assessments'}, state:{ c__apreset:'ready-to-book' } }` (contract: `docs/architecture/gtm-assessments-table.md` §3.x and its URL-state list; currently only in worktree `/Users/isiwintr/Documents/Workbench/worktrees/issue-assessments-tab-filter`, branch `agent/issue-assessments-tab-filter`). If that issue has not merged when this is built, **degrade to the unfiltered `GTM_Assessments` tab** (no `c__apreset`), guarded so a follow-up flips one constant/state; QA notes which mode shipped. Caveat for the Architect: `ready-to-book` is status New AND tier in (Fast-Track, Accelerator-Ready), a SUBSET of the ranked list (which shows all statuses), so the link label must not imply the tab shows the same set (suggest "View all / ready to book" wording or an alternative; human copy fork).
  - Explicitly NOT built: the "How ready are the prospects who submitted?" bar chart and the average-score line.
  - **Server source (OPEN FORK 2, decide with the Architect to avoid two ranking implementations).** Verified: `GtmAssessmentListController.getAssessmentPage(AssessmentQuery)` is specified in the assessments-tab-filter contract (default `best-fit` sort = tier rank DESC, score DESC, coalesce(submittedAt,createdDate) DESC, Id DESC; `pageSize` clamp 1..200; returns `rows` with `recordId, company, tier, tierRank, score, requestStatus, offeringKey, ...` and `totalCount`) but is **NOT on `main` yet** (no such class in `force-app/main/default/classes/` today). Recommendation: if it has merged, call it with `{ pageSize: 25 }` (default best-fit sort, no filters) and build NO new ranking query; its own doc (§ on `GtmHomeSnapshotController`) already says the Overview must move onto it and not copy `isAdmin()`. If it has not merged, the Architect must either (a) block C on it, or (b) ship a minimal interim ranker and file a follow-up to swap it out; (b) creates the two-implementation risk the owner wants avoided, so prefer (a). The per-offering submitted count can also come from `totalCount` with `offering:[key]`, but that is N calls; a single grouped aggregate is cheaper. Architect decides.
  - **STANDING RULE, Salesforce-native first, decision recorded here for the Architect to confirm:** evaluate `lightning-datatable` (or a standard related-list style component) for the ranked list. BA position: the assessments-tab-filter contract already chose `lightning-datatable` with `cellAttributes` (coloured text + icon, no custom type) for the full table, so the ranked list SHOULD use `lightning-datatable` too (`key-field`, `hide-checkbox-column`, fixed-height scroll container, first column a `button`-type cell for row open) unless the Architect shows it cannot meet the ~25-row compact card, the row-open navigation, or the keyboard/a11y requirements. A standard related list / list view cannot express the explicit tier-rank sort or the readout/status join, so it is rejected (same reasoning as the contract doc). The Architect must write the decision and any rejection reason into the architecture doc. If a hand-built list is chosen, justify it explicitly.

  **Scoping / security (rep-data-scoping-owd):** `GTM_Assessment_Request__c` and `GTM_Saved_Configuration__c` are Private OWD. `with sharing` plus explicit owner scoping so a rep sees only their own and an admin sees all, using a **generalized view-all check (`hasViewAllRecords` on the object, the owd-2/3 pattern), NOT the permission-set-name `GtmHomeSnapshotController.isAdmin()`** (the assessments contract says explicitly not to copy it). No DML anywhere (`@AuraEnabled` reads only). Preserve the dynamic-query handling of the `Saved_Configuration__c` lookup where existing code uses it (see comments at `countStalled`).

- **System Component Impacted:** LWC `gtmOverview` (`.html` Row 3, `.js`, `.css`, `__tests__/gtmOverview.test.js` and any new `__tests__/gtmOverview.row3.test.js`); new read-only Apex class (offering counts) plus its test class; possibly `GtmAssessmentListController` (consumed, owned by issue `assessments-tab-filter`); `GtmPageContentController.getHomeSummary` is read, not changed; permission sets `GTM_Offering_User` and `GTM_Offering_Admin`. No Experience Cloud route, no YAML instrument. Depends on piece A (layout skeleton) and, softly, on `assessments-tab-filter` and `GtmLinkStageService` (the merged stage service whose universe rules the live-link count copies; not present in `classes/` on `main` at scoping time, Architect to re-check before building, since a merged reference is assumed by the coordinator).

## 2. Code Dependency Checklist

- [ ] Modifying GUS Tool Surface? NO. All new Apex is read-only (SOQL only, zero DML), which satisfies the AGENTS.md §1 zero-DML rule; QA confirms no `insert/update/delete/upsert` in the new class. The future GUS tool over `getAssessmentPage` is a separate issue.
- [ ] Altering Custom Metadata? NO. `GTM_Offering__mdt` is read at runtime for labels only; nothing under `customMetadata/` and no `instrument/` YAML is touched.
- [ ] Introducing database fields? NO. `Offering__c`, `Presentation_Stage__c`, `Active__c`, `Offering_Key__c`, `Assessment_Tier__c`, `Assessment_Score__c`, `Status__c` all exist. BUT a NEW Apex class needs `classAccesses` (enabled) in `GTM_Offering_User` and `GTM_Offering_Admin` ONLY (not the Content sets, not `GTM_Guest`); `GtmAssessmentListController`, if consumed, is granted by its own issue, the Architect must verify it is on both sets before this ships. If a field becomes unavoidable, CLAUDE.md §6 applies (grant on User and Admin sets, never `fieldPermissions` on required/MasterDetail fields).

## 3. Plan Acceptance Criteria

- **Success Metric:** QA passes only when ALL hold.
  1. Layout: Offerings renders in Row 3 LEFT beside "Assessment results" in Row 3 RIGHT, and no longer renders at the bottom of the Overview; existing offering-card content and the story/feedback behaviour and button-compliance tests are unchanged.
  2. Offerings counts: every card shows its live-link count and submitted-assessment count from the Apex payload (not derived client-side); zero-count offerings still render with 0; raw-key fallback label when `GTM_Offering__mdt` has no match; if the counts call fails the cards still render without counts.
  3. Ranked list order: Fast-Track > Accelerator-Ready > Prep Required > Discovery First > null-tier last; score descending within a tier; newest first on equal score; null tier rows are shown (em dash), not dropped; the ranking is an explicit rank map (a test with a tier list that would sort differently alphabetically/by picklist position).
  4. Cap and scroll: at most ~25 rows requested/rendered, list lives in a fixed-height container with internal scroll (class assertion), sharing piece A's height treatment.
  5. Row click navigates `GTM_Assessments` with `c__assessmentRequestId`; "View all" navigates to `GTM_Assessments` with `c__apreset: 'ready-to-book'` (or, if `assessments-tab-filter` has not merged, the unfiltered tab, recorded in the QA report); rows/links are keyboard-operable.
  6. Empty states: no submitted assessments shows a friendly empty message (no chart, no error); no offerings shows the existing offerings empty state; counts error handled.
  7. The bar chart, "How ready are the prospects who submitted?" text, and average-score line do not exist anywhere in the template.
  8. The ranking has ONE implementation (either `getAssessmentPage` reused, or, only if unavoidable, a documented interim one with a follow-up ticket); the Architect's doc records the lightning-datatable decision.
  9. Apex scoping: two reps and an admin: rep A sees only A's links/assessments and counts, rep B only B's, a view-all user (admin) sees all, for both the counts and the list; Rep_Direct and Draft links excluded from live-link counts; no DML; class access granted on `GTM_Offering_User` and `GTM_Offering_Admin` only.
  10. No new hex colours beyond the four approved funnel values (those belong to piece A); tier/status colouring uses existing SLDS utilities/tokens.
  11. Live browser validation (standing QA rule) on Overview and every dependent surface: row click lands on the correct assessment in `GTM_Assessments`, "View all" lands on the tab in the filtered (or degraded unfiltered) state, rep vs admin views, offering card story/feedback actions still work. `gtm-dev` is Production: read-only checks or the existing synthetic seed only, no destructive scripts.
  12. CI header `ISSUE #overview-sales-dashboard-3-offerings-results` appears in every CI-required place.
- **Target Test Target:** Jest: `npx sfdx-lwc-jest -- force-app/main/default/lwc/gtmOverview` (existing `gtmOverview.test.js`, `gtmOverview.repDirect.test.js`, plus a new Row-3 spec covering list order, scroll-cap class, row click, view-all navigation, offerings count display, empty states, counts-error degrade). Apex: a new test class for the offering-counts class (ranking map, counts, Rep_Direct/Draft exclusion, label fallback, two-rep + admin scoping, no DML), and, if `getAssessmentPage` is reused, re-run `GtmAssessmentListControllerTest` to prove the shared ranking; run per the QA gate with `sf apex run test --class-names <NewCountsTest> --result-format human --wait 10` (note this executes against `gtm-dev`, treated as Production).

### Open forks for the Architect (summary)
1. Live-link definition: stage-service universe only, or also `Active__c = true`.
2. Results source: reuse `GtmAssessmentListController.getAssessmentPage` (preferred, block on its merge) vs interim ranker (two-implementations risk).
3. `lightning-datatable` vs hand-built list (BA leans datatable; Architect records decision).
4. "View all" wording, since `ready-to-book` is a subset of the list; and the degrade path if `c__apreset` has not merged.
5. Submitted-count query shape: one grouped aggregate vs N `getAssessmentPage` calls.
6. Whether Row 3 keeps the fixed height when stacked below the 1100px breakpoint (align with piece A's decision).
7. Whether the results list is time-windowed (parent spec says none; `getAssessmentPage` has no default window, all-time top 25 by best fit).
