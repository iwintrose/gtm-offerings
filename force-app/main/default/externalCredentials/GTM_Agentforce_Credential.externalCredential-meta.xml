<?xml version="1.0" encoding="UTF-8"?>
<!--
  Issue gus-live-agentforce-provider-auth. RE-RECONCILED against a second
  live `sf project retrieve` (2026-09-25, after the coordinator cleared the
  Scope field live to fix an "invalid_request: scope parameter not
  supported" token-issuance error). The Scope parameter present in the
  first reconciliation pass is gone from the live org and is intentionally
  removed here too. See docs/architecture/gus-chat-provider-settings.md
  §5a for the full reconciliation record, including why Scope had to be
  cleared and the still-separate question of the External Client App's
  own OAuth scopes (a different setting, on ExtlClntAppOauthSettings, not
  this file — see the doc for the scope gap found there).

  Secret-free by design (CLAUDE.md §4, gus-chat-provider-settings.md §2):
  the OAuth consumer secret AND consumer key are entered directly against
  this External Credential's Principal (GTM_Agentforce_Principal) in
  gtm-staging Setup > External Credentials > GTM_Agentforce_Credential >
  Principals — neither is part of this definition file, confirmed by two
  live retrieves (grepped for "secret"/"password"/a consumer-key
  parameter; the only match either time is the flow-type name
  `ClientCredentialsClientSecretBasic`, not a value).
-->
<ExternalCredential xmlns="http://soap.sforce.com/2006/04/metadata">
    <authenticationProtocol>Oauth</authenticationProtocol>
    <externalCredentialParameters>
        <parameterGroup>DefaultGroup</parameterGroup>
        <parameterName>Oauth</parameterName>
        <parameterType>AuthProtocolVariant</parameterType>
        <parameterValue>ClientCredentialsClientSecretBasic</parameterValue>
    </externalCredentialParameters>
    <externalCredentialParameters>
        <parameterGroup>DefaultGroup</parameterGroup>
        <parameterName>AuthProviderUrl</parameterName>
        <parameterType>AuthProviderUrl</parameterType>
        <parameterValue>https://pu1789790920110.my.salesforce.com/services/oauth2/token</parameterValue>
    </externalCredentialParameters>
    <externalCredentialParameters>
        <parameterGroup>GTM_Agentforce_Principal</parameterGroup>
        <parameterName>GTM_Agentforce_Principal</parameterName>
        <parameterType>NamedPrincipal</parameterType>
        <sequenceNumber>1</sequenceNumber>
    </externalCredentialParameters>
    <label>GTM Agentforce Credential</label>
</ExternalCredential>
