<?xml version="1.0" encoding="UTF-8"?>
<!--
  Issue gus-live-agentforce-provider-auth. RECONCILED against a live
  `sf project retrieve` of the real GTM_Agentforce_API Named Credential
  created in gtm-staging Setup (2026-09-25) — this is the actual metadata
  shape (note it differs from the first draft: allowMergeFieldsInBody/
  allowMergeFieldsInHeader/generateAuthorizationHeader/calloutStatus are
  top-level elements, not namedCredentialParameters entries, and the
  ExternalCredential link uses a nested <externalCredential> child rather
  than a parameterValue). See docs/architecture/gus-chat-provider-settings.md
  §5a for the full reconciliation record.

  STILL OPEN (per the coordinator's own flag): the Url below is the org's
  My Domain base (https://pu1789790920110.my.salesforce.com), NOT
  necessarily the correct Agent API host. Salesforce's Agent API has
  historically been served from a different host
  (api.salesforce.com/einstein/ai-agent/...) than My Domain in some
  documented versions, and the task-scope explicitly flags this surface as
  having "changed materially over 2025." Do NOT assume
  `callout:GTM_Agentforce_API/einstein/ai-agent/v1/...` resolves correctly
  until a live Apex callout through this Named Credential is smoke-tested
  end-to-end (open item, see §5a "Smoke-test evidence").
-->
<NamedCredential xmlns="http://soap.sforce.com/2006/04/metadata">
    <allowMergeFieldsInBody>false</allowMergeFieldsInBody>
    <allowMergeFieldsInHeader>false</allowMergeFieldsInHeader>
    <calloutStatus>Enabled</calloutStatus>
    <generateAuthorizationHeader>true</generateAuthorizationHeader>
    <label>GTM Agentforce API</label>
    <namedCredentialParameters>
        <parameterName>Url</parameterName>
        <parameterType>Url</parameterType>
        <parameterValue>https://pu1789790920110.my.salesforce.com</parameterValue>
    </namedCredentialParameters>
    <namedCredentialParameters>
        <externalCredential>GTM_Agentforce_Credential</externalCredential>
        <parameterName>ExternalCredential</parameterName>
        <parameterType>Authentication</parameterType>
    </namedCredentialParameters>
    <namedCredentialType>SecuredEndpoint</namedCredentialType>
</NamedCredential>
