<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Key__c</fullName>
    <label>Key</label>
    <description>Answer key. Must NOT collide with any readiness or complexity dimension key -- the build script fails the build if it does, because a supplement that lands on a dimension key would quietly enter the 32.</description>
    <required>true</required>
    <type>Text</type>
    <length>80</length>
    <unique>false</unique>
</CustomField>
