<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Question_Key__c</fullName>
    <label>Question Key</label>
    <description>Stable slug key for this question, referenced by other questions' Rules_JSON__c conditions/targets (e.g. branching rules point at this key, not the record Id, so rules survive record recreation/reordering).</description>
    <type>Text</type>
    <length>100</length>
    <required>false</required>
    <unique>false</unique>
</CustomField>
