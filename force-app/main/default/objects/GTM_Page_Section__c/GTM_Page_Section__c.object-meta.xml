<?xml version="1.0" encoding="UTF-8"?>
<CustomObject xmlns="http://soap.sforce.com/2006/04/metadata">
    <deploymentStatus>Deployed</deploymentStatus>
    <description>One row per section of a rendered page template. Holds the section's order, label and layout type, so a page's structure is data rather than markup. GTM_Page_Content__c rows attach to a section by Section_Key__c. Offering-agnostic: a new offering is a new set of rows, not new code.</description>
    <enableActivities>false</enableActivities>
    <enableHistory>true</enableHistory>
    <enableReports>true</enableReports>
    <enableSearch>true</enableSearch>
    <label>GTM Page Section</label>
    <nameField>
        <label>Section Name</label>
        <type>Text</type>
    </nameField>
    <pluralLabel>GTM Page Sections</pluralLabel>
    <sharingModel>ReadWrite</sharingModel>
</CustomObject>
