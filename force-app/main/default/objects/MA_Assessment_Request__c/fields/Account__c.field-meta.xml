<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Account__c</fullName>
    <label>Account</label>
    <description>Carried over from the Engagement Link's own Account, so the request shows the full chain (Account / Contact / Lead / Opportunity / Engagement Link) without having to hop through the link record.</description>
    <type>Lookup</type>
    <referenceTo>Account</referenceTo>
    <relationshipLabel>Assessment Requests</relationshipLabel>
    <relationshipName>MA_Assessment_Requests</relationshipName>
    <deleteConstraint>SetNull</deleteConstraint>
</CustomField>
